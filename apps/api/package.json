{
	"name": "@repo/api",
	"version": "0.0.1",
	"private": true,
	"description": "The Restock Express API",
	"keywords": [],
	"bugs": {
		"url": "https://github.com/bennobuilder/restock/issues"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/bennobuilder/restock.git"
	},
	"license": "AGPL-3.0-only",
	"author": "@bennobuilder",
	"type": "module",
	"exports": {
		"./openapi": "./src/openapi/index.ts",
		"./openapi.json": "./openapi.json",
		"./package.json": "./package.json"
	},
	"scripts": {
		"build": "shx rm -rf dist && esbuild src/server.ts --bundle --platform=node --target=node24 --format=esm --packages=external --outfile=dist/server.js",
		"clean": "shx rm -rf dist && shx rm -rf .turbo && shx rm -rf node_modules",
		"db:local:start": "docker compose up -d --wait mongodb",
		"db:local:stop": "docker compose stop mongodb",
		"install:clean": "pnpm run clean && pnpm install",
		"lint": "eslint .",
		"openapi:generate": "pnpm run openapi:generate:runtime && pnpm run openapi:generate:types",
		"openapi:generate:runtime": "tsx scripts/generate-openapi-runtime.ts",
		"openapi:generate:types": "openapi-typescript openapi.json -o src/openapi/api-v1.gen.ts",
		"start": "NODE_ENV=production node --env-file-if-exists=.env.local dist/server.js",
		"start:dev": "tsx watch --env-file-if-exists=.env.local src/server.ts",
		"test": "echo \"🧪 No tests defined yet.\"",
		"typecheck": "tsc --noEmit && tsc -p scripts/tsconfig.json",
		"update:latest": "pnpm update --latest"
	},
	"dependencies": {
		"cors": "^2.8.6",
		"express": "^5.2.1",
		"mongoose": "^9.7.4",
		"openapi-ts-router": "^0.4.1",
		"validatenv": "^0.1.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@blgc/config": "^0.1.0",
		"@types/cors": "^2.8.19",
		"@types/express": "^5.0.6",
		"@types/node": "^26.1.1",
		"esbuild": "^0.28.1",
		"openapi-typescript": "^7.13.0",
		"tsx": "^4.23.1"
	}
}
